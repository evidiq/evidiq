# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/dist

# secrets & env — NEVER commit
.env
.env.*
!.env.example

# raw access/credentials files + infra runbook (kept local only)
Akses Gitignore
EVIDIQ-RUNBOOK.md
*.pem
*.key

# testing / coverage
/coverage

# misc / OS / editor
.DS_Store
*.pem
.vscode/*
!.vscode/extensions.json
.idea
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# working logo source files (kept local only, never uploaded)
/Logo
/logo 2
/logo color.png
/evidiq.dev
/evidiq-avatar.png

# demo / video test outputs (kept local only, never uploaded)
*.mp4
/evidiq-demo.gif
/evidiq-demo.cast
